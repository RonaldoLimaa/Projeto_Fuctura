import { MaiusculoDirective } from './maiusculo.directive';

describe('MaiusculoDirective', () => {
  it('should create an instance', () => {
    const directive = new MaiusculoDirective();
    expect(directive).toBeTruthy();
  });
});
